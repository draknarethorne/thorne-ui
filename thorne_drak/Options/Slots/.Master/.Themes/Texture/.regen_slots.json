{
  "_comment": "Texture theme overrides for slot styling. Inherits from .Master/.regen_slots.json",
  "_note": "Add texture-specific gradient overrides here when ready. Currently mirrors master config.",
  "gradient_presets": {}
}
